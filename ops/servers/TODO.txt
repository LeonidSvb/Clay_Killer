# Server Dash — TODO

## Этап 1 — HTML Mockup (feedback first)

[x] Базовый мокап: Projects / Domains / Databases / Containers + drill-down
[x] Добавить Alerts panel на Level 0 (failed jobs, stale tables, orphaned DB)
[x] Добавить Services/Jobs — иерархия: проект → скрипты, каждый со своими метриками и run history
[x] Добавить Cleanup Candidates раздел (мёртвые проекты / таблицы / БД агрегированы)
[x] Получить ОС от Леонида по мокапу → перейти к этапу 2

## Этап 2 — React server-dash (после ОС)

[x] Добавить Service entity в types.ts (ScriptRun, Script, Service, Alert, CleanupItem)
[x] Добавить services[], alerts[], cleanupItems[] в data.ts
[x] Добавить ServicesPage — accordion (service → script → detail + runs table)
[x] Добавить run drill-down — inline панель с sample записей
[x] Добавить AlertsBanner на все list-views
[x] Добавить CleanupPage (high/medium severity, grouped by type)
[x] Починить инлайн-стили Topbar/Sidebar → чистый Tailwind dark: variants

## Этап 3 — Backend FastAPI (код готов, нужен деплой)

[x] GET /api/containers — docker ps -a + docker stats (15s cache)
[x] GET /api/databases — pg_stat_user_tables на shared-pg + supabase-db (60s cache)
[x] GET /api/status — cpu/ram/disk/uptime из /proc/stat + free + df
[x] AppContext в React — live fetch каждые 30s, fallback на mock если API недоступен
[x] Индикатор live/mock в топбаре (зелёная точка = live, серая = mock)

[ ] ДЕПЛОЙ: cp .env.example → .env, заполнить пароли из netcup-primary.env
[ ] ДЕПЛОЙ: docker compose up -d в /opt/compose/server-dash-api/
[ ] ДЕПЛОЙ: cp traefik-dash.yml → /data/coolify/proxy/dynamic/
[ ] ДЕПЛОЙ: docker network connect server-dash-api_default coolify-proxy
[ ] ДЕПЛОЙ: добавить DNS A-запись dash.pamelacoreypc.com → 152.53.194.162

## Этап 4 — Campaigns (outreach-cockpit — отдельный проект)

Относится к LeonidSvb/outreach-cockpit, не к server-dash.
Мокап готов: outreach-cockpit/docs/campaigns-mockup.html
Имплементировать когда придёт очередь того проекта.
